package ceng.estu.classes;

/**
 *
 * You can add types directly here. Program will automatically adjust choiceboxes.
 * <>p</>
 * For removing Type Make sure the config file does not have
 * any product in that type.
 *
 * For removing Food and Drink go to TableSectionController(125,126) and change default products
 * or remove them.
 * @author reuzun
 */
public enum Type {
    Food,
    Drink
}
